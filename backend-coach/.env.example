PORT=8787
# Local Day-1: skip Firebase Auth so Simulator can hit localhost without Cloud Run IAM.
COACH_SKIP_AUTH=1
# Fake stream without Gemini (plumbing check). Set 0 and provide GEMINI_API_KEY for real model.
COACH_MOCK_STREAM=0
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
